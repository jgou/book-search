services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: book-search-backend
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: book-search-frontend
    depends_on:
      - backend
    ports:
      - "3000:80"